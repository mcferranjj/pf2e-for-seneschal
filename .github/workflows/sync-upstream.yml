name: Sync Upstream

on:
  schedule:
    - cron: '0 0 * * *' # Runs daily at 00:00 UTC
  workflow_dispatch: # Allows manual triggering

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          ref: v14-dev # Target branch in your fork
          fetch-depth: 0

      - name: Sync upstream
        run: |
          git remote add upstream https://github.com/foundryvtt/pf2e.git
          git fetch upstream
          git checkout v14-dev
          git merge upstream/v14-dev
          git push origin v14-dev
        env:
          # Use a PAT to trigger subsequent workflows if needed
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
